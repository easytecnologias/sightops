# SightOps v3 -- roteamento por origem (tabelas por conector) exige rp_filter frouxo.
# Sem isso o kernel dropa a resposta que volta pela wgc<N> (validacao reversa cai na main).
net.ipv4.conf.all.rp_filter = 2
net.ipv4.conf.default.rp_filter = 2
